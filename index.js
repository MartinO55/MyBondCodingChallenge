function clickThroughStory(destination) //so we want this function to fade out the text we are hovering over and fade in the next line, and once its gone, its gone
{
    document.querySelector(destination)
    console.log("hi")
}
   